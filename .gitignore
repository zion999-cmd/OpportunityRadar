# dependencies
node_modules/

# build output (currently none, but guard for the future)
dist/
build/
*.tsbuildinfo

# environment / secrets
.env
.env.*
!.env.example

# editor / OS
.DS_Store
.idea/
.vscode/

# test / coverage artifacts
coverage/
.nyc_output/
*.log

# SQLite databases (created by `npm run db:init` / CLI smoke).
# The directory itself is tracked via .gitkeep; the DB files are not.
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Dev-time exploration artifacts (one .md per `explore` run).
# The directory is created on demand by the CLI; the files are
# observation windows for human review, not part of the commit.
artifacts/
