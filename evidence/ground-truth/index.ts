// Barrel for Ground Truth fixtures. P0001 §Ground Truth is a
// closed corpus used by the corpus-integrity contract test
// (tests/contract/ground-truth-corpus.test.ts) and by future
// demos / fixtures / seed data. The implementation agent owns
// changes to the corpus only inside an approved Proposal.

export { groundTruthSources } from './sources.js';
export { groundTruthEvidence } from './evidence.js';
