# P004 Evidence Result

Acquisition date: 2026-09-07
Decisive unknown: whether Jeremy Singer-Vine translated the static
claim-evidence architecture (as documented in the BuzzFeed News
investigation context) into an interactive surface navigated by a
non-author evaluating consequential AI output, **outside** that
BuzzFeed context.

## Outcome

**EVIDENCE_NOT_FOUND**

The acquisition route terminates as unresolved. Per the frozen plan,
EVIDENCE_NOT_FOUND is a successful experimental result and is reported
as such without weakening the evidence requirement.

## Artifact

None. No shipped interactive artifact attributable to Singer-Vine
outside the BuzzFeed News investigation context was identified in
this acquisition pass.

## Attribution

Not applicable — no artifact was acquired. The closest candidate
inspected (the Idyll interactive-document project) is publicly credited
to **Matt Conlen** and **Jeffrey Heer** at the University of
Washington's Interactive Data Lab, and Singer-Vine is not named on
the Idyll homepage, in the project's GitHub README, or in the
academic citation as retrieved in this session.

## Direct Evidence

None. The only public code directly attributable to the
`jeremysv` GitHub account is a 2017 fork of the Home Assistant
documentation Jekyll site — a static documentation project, not an
interactive claim-to-evidence system. The BuzzFeed News GitHub
organization catalog (116 repositories) was inspected and contains
no shipped interactive product, AI-output surface, or
claim-to-evidence system; in any case, that organization is the
out-of-scope context the frozen plan excludes.

## What the artifact allowed another person to inspect

Not applicable — no artifact was acquired.

## Disclosure / evidence boundary

Not applicable — no artifact was acquired.

## Remaining unknowns

Three concrete links in the evidence standard remained unsupported
after this acquisition pass:

1. **Existence link.** No public first-party or reputable secondary
   source retrieved in this session names a specific shipped
   interactive artifact, product surface, or shipped system
   attributable to Singer-Vine that allows a non-author to navigate
   from a consequential claim or AI-generated output to supporting
   computation, methodology, or evidence. The personal site
   `jeremysv.com` and several professional-bio pages
   (`muckrack.com`, `source.opennews.org`, `crunchbase.com`,
   `themarkup.org/about/staff`) were unreachable, returned 403/404,
   or did not render the relevant content in the fetched
   representation. The web-search backend was unavailable (auth
   failure) and `web.archive.org` was unreachable from the
   retrieval environment, so archived snapshots could not
   substitute.

2. **Attribution link.** The strongest external candidate matching
   the functional pattern (Idyll) is publicly attributed to Matt
   Conlen and Jeffrey Heer. The Idyll contributors graph and the
   project's GitHub API endpoint did not return contributor data
   in the unauthenticated fetches attempted here, so the absence
   of Singer-Vine from the contributor list is established from
   the README and homepage text rather than a complete contributor
   audit. The Idyll paper author list could not be retrieved
   directly (the IDL pages and the canonical arXiv preprint
   pointer were both unreachable in the session).

3. **Boundary / disclosure link.** Because no artifact was
   identified, no observation can be made about what information
   such an artifact exposed, withheld, reviewable, or otherwise
   bounded.

These three gaps are why the acquisition route terminates as
unresolved rather than as a positive finding. They are the
precise links that, if filled by a future pass with restored
search infrastructure, an accessible personal-site snapshot, or
authenticated access to GitHub contributor data, could promote a
candidate to acquired evidence.
